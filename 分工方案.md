# 软件测试实践小组分工方案

## 1. 项目范围

模块一将对项目中的三个 Python 文件进行测试：

- `mm_separators.py`：视觉 token 注意力排序、视觉 token 压缩、位置编码和 attention mask 更新。
- `cache.py`：`MMSepCache` 的初始化、KV cache 更新、分隔符筛选、缓存压缩、位置编码偏移和缓存接口。
- `auto_eval.py`：数据分块、JSONL 读取、提示词构造、评分结果解析、评估结果保存和异常处理。

`software_test.py` 和 `test.py` 用作统一测试入口或测试辅助文件，不作为主要被测模块。模块一的测试用例、测试脚本和测试数据由成员独立设计和实现，不使用 AI 生成测试用例或开展自动化测试。

模块二选择方案一“测 AI”：在模块一测试基础上接入一个可运行的多模态模型或模型替身，重点测试模型推理结果的鲁棒性、公平性和安全性。模型权重、密钥等大文件和敏感信息不提交到仓库。

## 2. 成员分工

以下使用“成员 A”和“成员 B”作为占位名称，提交时替换为真实姓名和 Git 账号。

### 成员 A：模型接口与评估流程负责人

负责 `auto_eval.py` 的完整测试，并协助统一测试工程和模块二模型接入。

具体工作：

1. 梳理 `auto_eval.py` 的输入格式、输出格式、评分模式、分块逻辑和异常处理需求。
2. 人工设计该文件的等价类、边界值和场景法测试用例，建议不少于 12 条。
3. 编写 `auto_eval.py` 的自动化测试，使用 mock/stub 隔离外部 Ark API、图片文件和网络依赖。
4. 覆盖正常输入、空文件、字段缺失、分块参数异常、评分格式异常、API 异常和结果落盘等场景。
5. 负责统一测试配置、一键运行入口、测试依赖说明和根目录 README 的运行部分。
6. 负责模块二的模型调用封装、测试数据组织和推理结果采集。
7. 负责模型鲁棒性测试的主要设计，例如图像扰动、问题改写、长文本输入和重复运行一致性。
8. 负责整合测试报告、提交模块一最终版本，并主讲模型接口、评估流程和测试结论。

### 成员 B：缓存与视觉压缩负责人

负责 `cache.py` 和 `mm_separators.py` 的完整测试，并协助模块二的 AI 质量分析。

具体工作：

1. 梳理 `MMSepCache` 的生命周期、参数约束、KV tensor 维度和缓存压缩规则。
2. 梳理 `graph_rank_separators` 的视觉 token 筛选、padding、位置编号和 attention mask 更新逻辑。
3. 人工设计两个文件的等价类、边界值和场景法测试用例，合计不少于 18 条。
4. 编写两个文件的自动化测试，使用小尺寸可控 tensor 构造测试数据，避免依赖真实大模型和 GPU。
5. 覆盖空缓存、单层/多层缓存、不同 batch size、不同序列长度、无分隔符、分隔符数量不一致、padding、缓存压缩和视觉 token 数量变化等场景。
6. 负责模块一缺陷的复现、最小化案例、修复验证和回归测试。
7. 负责模块二的模型输出质量分析，重点测试公平性、安全性、幻觉、视觉内容变化和异常输入处理。
8. 负责缺陷清单中的缓存/视觉压缩部分，以及演示视频中的缓存测试执行过程。

### 共同负责

1. 共同确认三个被测文件的需求、测试 oracle 和不可测试依赖，避免只按实现细节写测试。
2. 共同维护测试用例 Excel、缺陷清单 Word、测试报告 Word、PPT 和演示视频。
3. 互相交叉评审测试用例和测试代码：成员 A 复核 B 的缓存与视觉测试，成员 B 复核 A 的评估流程测试。
4. 共同完成至少 30 条模块一测试用例，其中至少覆盖等价类、边界值、场景法中的两种方法。
5. 共同发现并记录至少 3 个模块一有效缺陷；模块二方案一至少记录 1 个 AI 相关有效缺陷。
6. 共同完成现场展示、答辩和最终材料检查。贡献度初始按 A 50%、B 50%记录，最后按 Git 提交、测试代码、文档和展示的实际工作量调整，合计必须为 100%。

## 3. 交付物对应关系

### 模块一

| 交付物 | 成员 A | 成员 B | 共同要求 |
|---|---|---|---|
| 测试用例清单 Excel | `auto_eval.py` 不少于 12 条 | `cache.py` + `mm_separators.py` 不少于 18 条 | 总数不少于 30 条，覆盖至少两种测试设计方法 |
| 自动化测试工程 | 评估流程测试、mock、统一入口 | cache 和 separator 测试、tensor fixture | 一键运行全部测试，并在 README 写明环境和命令 |
| 缺陷清单 Word | 评估流程缺陷 | 缓存/视觉压缩缺陷 | 有效缺陷不少于 3 个，包含现象、步骤、修复验证 |
| 测试报告 Word | 范围、流程、执行结果 | 实现细节、缺陷分析 | 写明个人贡献度，避免与 PPT 直接复制 |
| PPT | 统筹结构和数据 | 提供技术图和典型缺陷 | 建议 10--12 页，突出关键数字和典型 Bug |
| 演示视频 | 评估流程和总测试入口 | cache/separator 测试执行 | 展示核心功能、测试执行和结果 |

### 模块二：方案一“测 AI”

| 交付物 | 成员 A | 成员 B | 共同要求 |
|---|---|---|---|
| AI 测试用例清单 | 鲁棒性、输入变化、重复性 | 公平性、安全性、幻觉和异常输出 | 不少于 15 条，注明模型、输入、预期和判定依据 |
| 自动化测试脚本 | 模型调用、数据集和结果保存 | 输出断言、统计和回归分析 | 支持无 API 时使用本地模型替身或固定响应复现 |
| 缺陷清单 | 模型接口和鲁棒性缺陷 | AI 输出质量缺陷 | 至少 1 个有效 AI 缺陷，包含复现和修复验证 |
| 测试报告 | AI 融合方案、实验流程 | AI 反思、限制和缺陷分析 | 说明模型版本、提示词、数据来源和评价标准 |
| PPT/视频 | 模型接入和结果展示 | 典型 AI 缺陷和对比实验 | 突出“测 AI”与传统软件测试的差异 |

## 4. 实施进度

### 模块一（截止第 4 次上机，9 月 15 日 23:00，最终以课堂通知为准）

1. 9 月 8--9 日：共同确认测试范围、接口预期、环境和测试数据格式。
2. 9 月 10--11 日：A 完成 `auto_eval.py` 用例和测试，B 完成 `cache.py`、`mm_separators.py` 用例和测试。
3. 9 月 12 日：首次完整执行，记录失败用例和候选缺陷。
4. 9 月 13 日：交叉评审、缺陷复现、修复验证和回归测试。
5. 9 月 14 日：完成 Excel、缺陷清单、报告、PPT 和视频，进行一次完整彩排。
6. 9 月 15 日：提交并参加现场展示。

### 模块二（截止最后一次上机，9 月 25 日 23:00，最终以课堂通知为准）

1. 9 月 16--17 日：确定模型或模型替身、输入数据、评价指标和安全边界。
2. 9 月 18--20 日：分别实现模型接入、鲁棒性、公平性和安全性测试。
3. 9 月 21--22 日：执行实验，记录输出、失败样例和可复现环境。
4. 9 月 23 日：复现并验证 AI 缺陷，完成回归测试和结果统计。
5. 9 月 24 日：完成模块二报告、PPT、视频和答辩问题准备。
6. 9 月 25 日：提交并参加现场展示。

## 5. Git 与质量要求

- 两名成员必须使用各自账号提交，不能由一人代提交；提交历史应持续反映真实工作。
- 提交信息使用清晰格式，例如 `test: add auto_eval input boundary cases`、`test: cover cache compression`、`docs: update module one report`。
- 每个阶段至少保留需求分析、测试用例、测试实现、缺陷修复、文档和最终整合等多个有意义的提交。
- 每次合并前运行全部测试，并在提交说明或报告中记录测试命令、通过数量、失败数量和环境信息。
- 仓库中不得提交 API key、模型私有权重、个人路径和包含隐私的数据；使用配置模板和脱敏样例替代。
- 对外部 API、GPU 和大模型依赖提供 mock、CPU 小样本或固定响应，使教师可以复现实验。
