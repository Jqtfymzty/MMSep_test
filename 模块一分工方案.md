# 模块一分工方案

## 测试对象

测试 `auto_eval.py`、`cache.py`、`mm_separators.py`。使用小样例、mock 和 CPU tensor，不依赖真实 API、GPU 或大模型权重。`software_test.py` 作为统一测试入口。

## 成员 A：`auto_eval.py` 与测试整合

- 测试数据分块、JSONL 读写、提示词生成、评分解析、异常处理和结果保存。
- 编写不少于 12 条测试用例，覆盖正常输入、空输入、字段缺失、非法参数、错误评分和 API 异常。
- 在 `software_test.py` 中提供一键运行全部测试的入口。
- 准备 mock API 和最小 JSONL 样例，不调用真实 Ark 服务。
- 负责 README 中的环境配置和测试运行说明。
- 提交该模块测试代码、测试结果和至少 1 个有效缺陷记录。

## 成员 B：`cache.py`、`mm_separators.py`

- 测试缓存初始化、KV 追加、缓存压缩、分隔符筛选、padding、层索引和缓存接口。
- 测试视觉 token 排序、筛选数量、原始顺序、position ids 和 attention mask 更新。
- 编写不少于 18 条测试用例，使用固定的小尺寸 tensor 检查形状、长度、索引和关键值。
- 覆盖空输入、边界长度、无分隔符、分隔符数量不一致、非法维度和不同 batch 场景。
- 提交两个模块的测试代码、tensor fixture、测试结果和至少 2 个有效缺陷记录。

## 两人共同完成

- 合并为至少 30 条测试用例，覆盖等价类、边界值、场景法中的至少两种。
- 互相检查测试代码，并共同确认至少 3 个有效缺陷的复现和修复结果。
- 完成测试用例 Excel、缺陷清单、测试报告、PPT 和演示视频。
- 报告记录测试命令、通过数量、失败数量、环境信息和成员贡献度。
- 每人使用自己的 Git 账号提交，提交记录应能对应本人负责的代码和文档。

## 执行顺序

1. A 搭建统一入口和样例数据，B 准备 tensor fixture。
2. 两人分别编写正常、边界和异常测试。
3. 运行全部测试，记录并复现缺陷。
4. 修复后回归测试，合并文档并录制演示视频。

## 完成标准

一条命令可运行全部测试；用例不少于 30 条；有效缺陷不少于 3 个且完成验证；测试不依赖联网、API key、GPU 或大模型权重。
